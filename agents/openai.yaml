interface:
  display_name: "Frontend Gen"
  short_description: "Generate & extend maintainable React/Next frontend projects"
  default_prompt: "Use $frontend-gen to turn these references into a maintainable Next.js pure-frontend project."
policy:
  allow_implicit_invocation: true
